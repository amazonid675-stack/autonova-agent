# GitHub

Autonova supports public repository inspection and confirmation-gated hosted GitHub operations for issues, branches, and pull requests. Fine-grained GitHub token material is encrypted server-side and never returned to Android.

Local cloning, editing, committing, conflict resolution, releases, Actions control, and direct phone-side Git execution are not currently implemented. Any future local workspace must remain application-scoped and avoid unrestricted Android shell access.
